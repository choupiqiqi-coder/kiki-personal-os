import "server-only";import type { AiProvider,StructuredRequest,StructuredResult } from "../types";
export class AnthropicProvider implements AiProvider{readonly id="anthropic" as const;constructor(readonly model:string,private readonly apiKey=process.env.ANTHROPIC_API_KEY){}isConfigured(){return Boolean(this.apiKey);}async generateStructured(request:StructuredRequest):Promise<StructuredResult>{if(!this.apiKey)throw new Error("Anthropic API Key 未配置");const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),request.timeoutMs);const started=Date.now();try{const response=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:{"x-api-key":this.apiKey,"anthropic-version":"2023-06-01","content-type":"application/json"},signal:controller.signal,body:JSON.stringify({model:this.model,max_tokens:request.maxOutputTokens,temperature:request.temperature,system:request.system,messages:[{role:"user",content:request.input}],tools:[{name:"submit_analysis",description:"提交符合指定结构的最终分析报告",input_schema:request.schema}],tool_choice:{type:"tool",name:"submit_analysis"}})});const payload=await response.json() as {error?:{message?:string};content?:Array<{type:string;name?:string;input?:Record<string,unknown>;text?:string}>;usage?:{input_tokens?:number;output_tokens?:number}};if(!response.ok)throw new Error(payload.error?.message??`Anthropic 请求失败 (${response.status})`);const block=payload.content?.find(item=>item.type==="tool_use"&&item.name==="submit_analysis");if(!block?.input)throw new Error("Provider 未返回结构化分析");const inputTokens=payload.usage?.input_tokens??null;const outputTokens=payload.usage?.output_tokens??null;return{data:block.input,provider:this.id,model:this.model,latencyMs:Date.now()-started,usage:{inputTokens,outputTokens,totalTokens:inputTokens!==null&&outputTokens!==null?inputTokens+outputTokens:null}};}finally{clearTimeout(timer);}}}
