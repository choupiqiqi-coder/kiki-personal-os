import Link from "next/link";import { notFound } from "next/navigation";import { LogForm } from "@/components/learning/log-form";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { saveLogAction } from "../../../actions";
export default async function EditLog({params}:{params:Promise<{id:string}>}){const{id}=await params;const user=await requireUser();const data=await createDataAccess();const[log,skills]=await Promise.all([data.learning.logs.get(user.id,id),data.learning.skills.list(user.id)]);if(!log)notFound();return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28"><Link href="/learning/logs" className="text-sm text-muted-foreground">← 学习记录</Link><h1 className="mt-5 text-3xl font-semibold">编辑学习记录</h1><div className="mt-8"><LogForm log={log} skills={skills} action={saveLogAction.bind(null,id)}/></div></main>}
