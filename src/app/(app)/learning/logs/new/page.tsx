import Link from "next/link";import { LogForm } from "@/components/learning/log-form";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { saveLogAction } from "../../actions";
export default async function NewLog({searchParams}:{searchParams:Promise<{skillId?:string}>}){const{skillId}=await searchParams;const user=await requireUser();const data=await createDataAccess();const skills=await data.learning.skills.list(user.id);return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28"><Link href="/learning/logs" className="text-sm text-muted-foreground">← 学习记录</Link><h1 className="mt-5 text-3xl font-semibold">新增学习记录</h1>{skills.length?<div className="mt-8"><LogForm skills={skills} skillId={skillId} action={saveLogAction.bind(null,null)}/></div>:<p className="mt-8">请先创建技能。</p>}</main>}
