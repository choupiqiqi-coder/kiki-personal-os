import Link from "next/link";import { SkillForm } from "@/components/learning/skill-form";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { saveSkillAction } from "../../actions";
export default async function NewSkill(){const user=await requireUser();const data=await createDataAccess();await data.learning.categories.ensure(user.id);const categories=await data.learning.categories.list(user.id);return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28"><Link href="/learning" className="text-sm text-muted-foreground">← 返回学习中心</Link><h1 className="mt-5 text-3xl font-semibold">新建技能</h1><div className="mt-8"><SkillForm categories={categories} action={saveSkillAction.bind(null,null)}/></div></main>}
