import Link from "next/link";import { notFound } from "next/navigation";import { SkillForm } from "@/components/learning/skill-form";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { saveSkillAction } from "../../../actions";
export default async function EditSkill({params}:{params:Promise<{id:string}>}){const{id}=await params;const user=await requireUser();const data=await createDataAccess();const[skill,categories]=await Promise.all([data.learning.skills.get(user.id,id),data.learning.categories.list(user.id)]);if(!skill)notFound();return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28"><Link href={`/learning/skills/${id}`} className="text-sm text-muted-foreground">← 返回技能</Link><h1 className="mt-5 text-3xl font-semibold">编辑技能</h1><div className="mt-8"><SkillForm skill={skill} categories={categories} action={saveSkillAction.bind(null,id)}/></div></main>}
