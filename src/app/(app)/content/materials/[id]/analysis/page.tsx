import Link from "next/link";import { notFound } from "next/navigation";import { AnalysisReport } from "@/components/ai/analysis-report";import { providerStatus } from "@/server/ai/config";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { analyzeMaterialAction } from "./actions";
export default async function MaterialAnalysis({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const {id}=await params;const {error}=await searchParams;const user=await requireUser();const data=await createDataAccess();const [item,artifacts]=await Promise.all([data.content.materials.get(user.id,id),data.aiArtifacts.listByType(user.id,`viral_material_analysis:${id}`)]);if(!item)notFound();const latest=artifacts[0];const run=latest?.run_id?await data.aiArtifacts.getRun(user.id,latest.run_id):null;const active=providerStatus().find(x=>x.active);return <main className="mx-auto w-full max-w-2xl px-4 py-6 pb-28 sm:px-6"><Link href="/content/materials" className="text-sm text-muted-foreground">← 返回素材</Link><p className="mt-6 text-sm font-semibold text-violet-700">AI VIDEO BREAKDOWN</p><h1 className="mt-2 text-3xl font-semibold">{item.title}</h1><p className="mt-2 text-sm text-muted-foreground">{active?.label} · {active?.model}</p>{error?<div className="mt-5 rounded-2xl bg-red-50 p-4 text-sm text-red-700">分析失败：{error}</div>:null}{!active?.configured?<div className="mt-5 rounded-3xl bg-amber-50 p-5 text-sm text-amber-800">AI Provider 未配置，不会生成假分析。</div>:null}<form action={analyzeMaterialAction.bind(null,id)} className="mt-5"><button disabled={!active?.configured} className="min-h-13 w-full rounded-2xl bg-violet-700 font-semibold text-white disabled:opacity-40">{latest?"重新分析":"开始 AI 拆解"}</button></form>{latest?<div className="mt-7"><AnalysisReport artifact={latest} run={run}/><h2 className="mt-6 font-semibold">历史版本</h2><div className="mt-3 space-y-2">{artifacts.map(x=><div key={x.id} className="rounded-2xl bg-surface p-4 text-sm">v{x.version} · {new Date(x.generated_at).toLocaleString("zh-CN")}</div>)}</div></div>:null}</main>}
