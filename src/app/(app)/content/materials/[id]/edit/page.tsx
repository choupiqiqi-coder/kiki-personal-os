import Link from "next/link";import { notFound } from "next/navigation";import { MaterialForm } from "@/components/content/material-form";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { saveMaterialAction } from "../../../actions";
export default async function EditMaterial({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await requireUser();const data=await createDataAccess();const item=await data.content.materials.get(user.id,id);if(!item)notFound();return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28 sm:px-6"><Link href="/content/materials" className="text-sm text-muted-foreground">← 返回素材</Link><h1 className="mt-5 text-3xl font-semibold">编辑素材</h1><div className="mt-8"><MaterialForm item={item} action={saveMaterialAction.bind(null,id)}/></div></main>}
