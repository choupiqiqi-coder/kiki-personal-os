import Link from "next/link"; import { notFound } from "next/navigation"; import { InspirationForm } from "@/components/content/inspiration-form"; import { requireUser } from "@/server/auth/current-user"; import { createDataAccess } from "@/server/data"; import { saveInspirationAction } from "../../../actions";
export default async function EditInspiration({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await requireUser();const data=await createDataAccess();const item=await data.content.inspirations.get(user.id,id);if(!item)notFound();return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28 sm:px-6"><Link href="/content/inspirations" className="text-sm text-muted-foreground">← 返回灵感</Link><h1 className="mt-5 text-3xl font-semibold">编辑灵感</h1><div className="mt-8"><InspirationForm item={item} action={saveInspirationAction.bind(null,id)}/></div></main>}
