import Link from "next/link";import { notFound } from "next/navigation";import { TopicForm } from "@/components/content/topic-form";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { saveTopicAction } from "../../../actions";
export default async function EditTopic({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await requireUser();const data=await createDataAccess();const topic=await data.content.topics.get(user.id,id);if(!topic)notFound();return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28 sm:px-6"><Link href={`/content/topics/${id}`} className="text-sm text-muted-foreground">← 返回选题</Link><h1 className="mt-5 text-3xl font-semibold">完善选题</h1><div className="mt-8"><TopicForm item={topic} action={saveTopicAction.bind(null,id)}/></div></main>}
