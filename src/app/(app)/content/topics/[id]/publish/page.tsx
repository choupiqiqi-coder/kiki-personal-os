import Link from "next/link";import { notFound } from "next/navigation";import { Field,inputClass,textareaClass,FormActions } from "@/components/content/field";import { MetricFields } from "@/components/content/metric-fields";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { publishTopicAction } from "../../../actions";
export default async function PublishTopic({params}:{params:Promise<{id:string}>}){const {id}=await params;const user=await requireUser();const data=await createDataAccess();const topic=await data.content.topics.get(user.id,id);if(!topic)notFound();return <main className="mx-auto w-full max-w-xl px-4 py-6 pb-28 sm:px-6"><Link href={`/content/topics/${id}`} className="text-sm text-muted-foreground">← 返回选题</Link><h1 className="mt-5 text-3xl font-semibold">标记为已发布</h1><p className="mt-2 text-muted-foreground">{topic.title}</p><form action={publishTopicAction.bind(null,id)} className="mt-8 space-y-5"><Field label="发布平台"><input name="platform" required className={inputClass} placeholder="抖音 / 小红书 / YouTube"/></Field><Field label="发布时间"><input name="publishedAt" type="datetime-local" required className={inputClass}/></Field><Field label="作品链接"><input name="contentUrl" type="url" className={inputClass} placeholder="https://"/></Field><Field label="发布备注"><textarea name="notes" rows={3} className={textareaClass}/></Field><div className="border-t border-border pt-6"><h2 className="mb-5 text-xl font-semibold">首份数据快照</h2><MetricFields/></div><FormActions label="保存发布与数据"/></form></main>}
