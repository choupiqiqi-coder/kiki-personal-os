import Link from "next/link";import { Field,FormActions,inputClass,textareaClass } from "@/components/content/field";import { getLocalDate } from "@/lib/date";import { savePeriodAction } from "../../actions";
export default function NewPeriodPage(){return <main className="mx-auto max-w-2xl px-4 py-6 pb-28"><Link href="/health/periods" className="text-sm text-muted-foreground">← 生理周期</Link><h1 className="my-5 text-3xl font-semibold">记录生理期</h1><form action={savePeriodAction.bind(null,null)} className="space-y-5"><Field label="开始日期"><input name="startDate" type="date" required defaultValue={getLocalDate()} className={inputClass}/></Field><Field label="结束日期（可后补）"><input name="endDate" type="date" className={inputClass}/></Field><Field label="经量"><select name="flowLevel" className={inputClass}><option value="">不记录</option><option value="light">少</option><option value="medium">中</option><option value="heavy">多</option></select></Field><Field label="症状（逗号分隔，可选）"><input name="symptoms" placeholder="腹胀，疲劳" className={inputClass}/></Field><Field label="备注"><textarea name="notes" rows={3} className={textareaClass}/></Field><FormActions label="保存周期"/></form></main>}
