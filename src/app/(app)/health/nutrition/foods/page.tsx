import Link from "next/link";import { Field,FormActions,inputClass } from "@/components/content/field";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { deleteFoodAction,saveFoodAction } from "../../actions";
export default async function FoodsPage(){const user=await requireUser();const data=await createDataAccess();const foods=await data.health.foods.list(user.id);return <main className="mx-auto max-w-3xl px-4 py-6 pb-28"><Link href="/health/nutrition" className="text-sm text-muted-foreground">← 饮食记录</Link><h1 className="my-5 text-3xl font-semibold">常吃食物</h1><form action={saveFoodAction.bind(null,null)} className="space-y-4 rounded-3xl bg-surface-muted p-5"><Field label="食物名称"><input name="name" required className={inputClass}/></Field><div className="grid grid-cols-2 gap-3"><Field label="默认单位"><input name="defaultUnit" defaultValue="g" className={inputClass}/></Field><Field label="每100g热量"><input name="caloriesPer100g" type="number" step="0.1" required className={inputClass}/></Field><Field label="蛋白质/100g"><input name="proteinPer100g" type="number" step="0.1" className={inputClass}/></Field><Field label="碳水/100g"><input name="carbsPer100g" type="number" step="0.1" className={inputClass}/></Field><Field label="脂肪/100g"><input name="fatPer100g" type="number" step="0.1" className={inputClass}/></Field></div><FormActions label="保存到 Food Library"/></form><div className="mt-5 space-y-2">{foods.map(x=><article key={x.id} className="flex justify-between rounded-2xl bg-surface p-4"><div><h2 className="font-medium">{x.name}</h2><p className="text-sm text-muted-foreground">{x.calories_per_100g} kcal / 100g</p></div><form action={deleteFoodAction.bind(null,x.id)}><button className="text-sm text-red-500">删除</button></form></article>)}</div></main>}
