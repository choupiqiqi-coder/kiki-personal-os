import { FundForm } from "@/components/finance/fund-form";import { FinanceNav } from "@/components/finance/finance-nav";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const user=await requireUser();const data=await createDataAccess();const fund=await data.funds.get(user.id,id);return <main className="mx-auto max-w-2xl px-4 py-6 pb-28"><p className="text-sm font-semibold text-primary">MY FUNDS</p><h1 className="mt-2 text-3xl font-semibold">编辑基金</h1><FinanceNav/><FundForm fund={fund}/></main>}
