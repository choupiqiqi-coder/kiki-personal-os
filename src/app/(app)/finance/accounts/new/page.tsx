import Link from "next/link";import { AccountForm } from "@/components/finance/account-form";import { saveAccountAction } from "../../actions";export default function NewAccount(){return <main className="mx-auto max-w-2xl px-4 py-6 pb-28"><Link href="/finance/accounts" className="text-sm text-muted-foreground">← 资产账户</Link><h1 className="my-5 text-3xl font-semibold">新建账户</h1><AccountForm action={saveAccountAction.bind(null,null)}/></main>}
