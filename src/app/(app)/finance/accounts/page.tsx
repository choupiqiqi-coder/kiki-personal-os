import Link from "next/link";import { FinanceNav } from "@/components/finance/finance-nav";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { deleteAccountAction } from "../actions";
export default async function AccountsPage(){const user=await requireUser();const data=await createDataAccess();const rows=await data.finance.accounts.list(user.id);return <main className="mx-auto max-w-3xl px-4 py-6 pb-28"><header className="flex justify-between"><h1 className="text-3xl font-semibold">资产账户</h1><Link href="/finance/accounts/new" className="rounded-2xl bg-primary px-4 py-3 text-white">＋ 账户</Link></header><FinanceNav/><div className="mt-6 space-y-3">{rows.map(x=><article key={x.id} className="flex justify-between rounded-3xl border border-border bg-surface p-5"><div><h2 className="font-semibold">{x.name}</h2><p className="mt-1 text-sm text-muted-foreground">{x.account_type} · {x.currency} · {x.include_in_total?'计入总资产':'不计入总资产'}</p></div><form action={deleteAccountAction.bind(null,x.id)}><button className="text-sm text-red-500">删除</button></form></article>)}</div></main>}
