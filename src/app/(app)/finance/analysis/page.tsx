import { FinanceNav } from "@/components/finance/finance-nav";import { requireUser } from "@/server/auth/current-user";import { createDataAccess } from "@/server/data";import { getMarketProviderStatus } from "@/server/market/config";
export default async function AnalysisPage(){const user=await requireUser();const data=await createDataAccess();const o=await data.finance.overview(user.id);const status=getMarketProviderStatus();return <main className="mx-auto max-w-3xl px-4 py-6 pb-28"><h1 className="text-3xl font-semibold">投资分析</h1><FinanceNav/><section className="mt-6 rounded-3xl bg-surface p-5"><p className="text-sm text-primary">Market Data Provider</p><h2 className="mt-2 text-xl font-semibold">{status.message}</h2><p className="mt-2 text-sm text-muted-foreground">未配置时不会生成假行情，也不会把旧数据描述为实时行情。</p></section><section className="mt-4 rounded-3xl border border-border bg-surface p-5"><h2 className="font-semibold">AI 分析入口</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">已准备最小上下文：{o.positions.length} 个持仓、{o.tags.length} 个行业/主题标签、{o.transactions.length} 条流水、最近有效行情时间。当前仅保留入口，不主动调用 AI、不修改交易流水。</p><button disabled className="mt-4 w-full rounded-2xl bg-surface-muted p-4 text-sm text-muted-foreground">等待有效行情 Provider 与 AI Key</button><p className="mt-3 text-center text-xs">AI 分析，不构成投资建议</p></section></main>}
